# Activity 16: Running updateToTag to Deploy Changesets to the Tag

# Step 6: Run the updateToTag command
liquibase  --defaultsFile=../1/liquibase.properties --changeLogFile=xml.xml updateToTag release1