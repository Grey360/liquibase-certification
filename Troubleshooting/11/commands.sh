# Activity 11: Checking the Status of Pending changesets

# Step 3: Run the status command to check your pending changesets
liquibase  --defaultsFile=../1/liquibase.properties --changeLogFile=sql.sql status --verbose